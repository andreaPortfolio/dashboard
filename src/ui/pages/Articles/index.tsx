import { Flex } from "../..";
import { Articles } from "../..";

const ArticlesPage = () => {
  return (
    <Flex justifyContent="center">
      <Articles />
    </Flex>
  );
};

export default ArticlesPage;
